#include "holberton.h"

/**
* _abs - return the absolute value of input n
*@n: the number to check
*
* Return: return the absolute value of n
**/

int	_abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
